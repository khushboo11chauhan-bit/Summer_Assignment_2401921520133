class Solution {
    public void reverseString(char[] s) {
        int start =0;
        int e=s.length-1;// e means end 
        while(start<e){
        char temp = s[e];
        s[e] = s[start];
        s[start]= temp;
        e--;
        start++;
        }
        
    }
}
